#include <stdio.h>
#include <math.h>

int main() {
  float co, ca;
  float hipotenusa;

  printf("Informe o valor de \"CO\" e \"CA\": ");
  scanf("%f" "%f", &co, &ca);

  hipotenusa = sqrt(pow(co, 2) + pow(ca, 2));


  printf("%f11", hipotenusa);


  return 0;
}